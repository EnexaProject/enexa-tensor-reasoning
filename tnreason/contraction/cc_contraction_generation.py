import numpy as np

def negation_constant(variableColors, variableShape, dataColors, dataShapes):
    pass

def create_formulaProcedure(expression, formulaKey):
    addCoreKey = str(formulaKey) + "_" + str(expression) + "_d"
